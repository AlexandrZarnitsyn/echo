{
  "name": "full-messenger-fixed",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/server",
    "apps/client",
    "apps/desktop"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w apps/server\" \"npm run dev -w apps/client\" \"npm run dev -w apps/desktop\"",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
